services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    volumes:
      - ./backend/app:/app/app
    env_file:
      - .env
    depends_on:
      - dynamodb-local
      - redis
      - livekit
    networks:
      - commonality

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    volumes:
      - ./frontend/src:/app/src
      - ./frontend/public:/app/public
    env_file:
      - .env
    environment:
      - NEXT_PUBLIC_API_URL=${NEXT_PUBLIC_API_URL}
      - NEXT_PUBLIC_WS_URL=${NEXT_PUBLIC_WS_URL}
      - NEXT_PUBLIC_LIVEKIT_URL=${NEXT_PUBLIC_LIVEKIT_URL}
      - NEXT_PUBLIC_PASSWORD_MIN_LENGTH=${NEXT_PUBLIC_PASSWORD_MIN_LENGTH}
    depends_on:
      - backend
    networks:
      - commonality

  dynamodb-local:
    image: amazon/dynamodb-local:latest
    container_name: commonality-dynamodb
    ports:
      - "8000:8000"
    command: "-jar DynamoDBLocal.jar -sharedDb -dbPath /data"
    user: root
    volumes:
      - commonality-data:/data
    healthcheck:
      test: ["CMD-SHELL", "curl -s http://localhost:8000/shell/ || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 3
    networks:
      - commonality

  dynamodb-admin:
    image: aaronshaf/dynamodb-admin
    container_name: commonality-dynamodb-admin
    ports:
      - "8001:8001"
    env_file:
      - .env
    environment:
      DYNAMO_ENDPOINT: ${DYNAMODB_ENDPOINT}
      AWS_ACCESS_KEY_ID: ${AWS_ACCESS_KEY_ID}
      AWS_SECRET_ACCESS_KEY: ${AWS_SECRET_ACCESS_KEY}
      AWS_REGION: ${AWS_REGION}
    depends_on:
      dynamodb-local:
        condition: service_healthy
    networks:
      - commonality

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
    networks:
      - commonality

  livekit:
    image: livekit/livekit-server:latest
    ports:
      - "7880:7880"
      - "7881:7881"
      - "7882:7882/udp"
    command: --dev --bind 0.0.0.0
    environment:
      LIVEKIT_KEYS: "devkey: devsecret"
      LIVEKIT_LOG_LEVEL: info
    networks:
      - commonality

volumes:
  commonality-data:

networks:
  commonality:
    driver: bridge
